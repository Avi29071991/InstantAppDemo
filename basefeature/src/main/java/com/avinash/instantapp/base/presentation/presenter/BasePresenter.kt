package com.avinash.instantapp.base.presentation.presenter

/**
 * Base presenter for the application which contains method which will be used by other non-base feature modules
 */
interface BasePresenter {
    fun destroy()
}